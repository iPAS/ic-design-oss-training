# xschemrc is Tcl, not shell: environment variables are read with $env(...).
# Read through bin/xschem.sh (--rcfile).  PDK_ROOT and PDK come from tools/env.sh.
source $env(PDK_ROOT)/$env(PDK)/libs.tech/xschem/xschemrc
