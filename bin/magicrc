# magic reads this through bin/magic.sh (-rcfile).  PDK_ROOT and PDK come from
# tools/env.sh; the PDK's own magicrc sets the tech, cell paths and DRC styles.
source $env(PDK_ROOT)/$env(PDK)/libs.tech/magic/$env(PDK).magicrc
